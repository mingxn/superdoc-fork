{
  "name": "@superdoc/layout-bridge",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean --tsconfig tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "dependencies": {
    "@superdoc/contracts": "*",
    "@superdoc/layout-engine": "*",
    "@superdoc/measuring-dom": "*",
    "@superdoc/painter-dom": "*"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsup": "^8.0.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
